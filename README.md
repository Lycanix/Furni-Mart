# Furni-Mart
A static furniture e-commerce website built with vanilla JavaScript, using localStorage for client-side state management and dummy data.

landing page template [x]
navbar template [x]
products template [x]
product detail template [x]
wishlist template [x]